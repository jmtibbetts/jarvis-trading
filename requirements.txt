# Web framework
fastapi==0.115.12
uvicorn[standard]==0.29.0
jinja2==3.1.4
python-multipart==0.0.9
aiofiles==23.2.1

# Scheduling
apscheduler==3.10.4

# Database
sqlalchemy==2.0.30
aiosqlite==0.20.0

# HTTP
httpx==0.27.0
# 3.14.3 pulled in by cryptofeed; the 3.9.5 pin would make a fresh
# install unresolvable against it. The full suite runs on 3.14.3.
aiohttp==3.14.3
feedparser==6.0.11
websockets==17.0.1

# TA backends. The "installed separately by start.bat" era predates
# TA-Lib 0.6, which ships prebuilt wheels (C library bundled) for
# win/manylinux — so it pins here like everything else. Without it, CI
# ran the backtester on a gutted indicator stack and classified a clean
# synthetic uptrend as Short; the `ta` fallback pins alongside so the
# degraded path ta_engine.py names actually exists wherever this
# installs.
TA-Lib==0.6.8
ta==0.11.0
pandas==2.2.2
numpy==1.26.4
alpaca-py==0.26.0

# Telegram
python-telegram-bot==21.3

# Utilities
python-dotenv==1.0.1
massive==2.8.0
pdfplumber==0.11.10
pydantic==2.11.4
pydantic-settings==2.7.0
pytz==2024.1

yfinance>=0.2.40

# Phase 5 cryptofeed prototype (Windows: order_book needs vendor/patches/order_book-1.0.1-msvc.patch)
cryptofeed==2.5.0
pyflakes>=3.0        # undefined-name guard (tests/test_no_undefined_names.py)
