--- a/orderbook/utils.h	2026-08-07 17:04:35.767610000 -0700
+++ b/orderbook/utils.h	2026-08-14 05:24:43.649503000 -0700
@@ -10,7 +10,13 @@
 #include <stdint.h>
 #include <stddef.h>
 
+/* MSVC has no __builtin_expect; it is purely a branch-prediction hint,
+   so the portable definition is the expression itself. */
+#if defined(_MSC_VER)
+#define EXPECT(EXPR, VAL) (EXPR)
+#else
 #define EXPECT(EXPR, VAL) __builtin_expect((EXPR), (VAL))
+#endif
 
 
 enum side_e {
--- a/orderbook/utils.c	2026-08-07 18:02:03.449288800 -0700
+++ b/orderbook/utils.c	2026-08-14 05:22:09.033946900 -0700
@@ -216,5 +216,28 @@
 }
 
 #else
-#error "Unsupported architecture. Please contact the author to have support added"
+/* Portable software fallback (e.g. MSVC, which defines _M_X64 rather than
+   __x86_64__ and lacks the GCC target attributes above). Same reflected
+   CRC-32 (poly 0xEDB88320) as the accelerated paths, bit-serial. The
+   library's own design already anticipates hardware-less operation via
+   crc32_init() — this branch simply makes the compile match the design
+   instead of refusing. */
+int crc32_init(void)
+{
+    return 0;
+}
+
+uint32_t crc32(const uint8_t *data, size_t len)
+{
+    uint32_t crc = 0xFFFFFFFF;
+
+    while (len--) {
+        crc ^= *data++;
+        for (int bit = 0; bit < 8; bit++) {
+            crc = (crc >> 1) ^ (0xEDB88320 & (uint32_t)-(int32_t)(crc & 1));
+        }
+    }
+
+    return ~crc;
+}
 #endif
